---
layout: layouts/base.njk
templateClass: tmpl-home
---

{# This is the template for individual blog posts–it builds the front matter properties into the page #}

  <div class="content">
      {# The post front matter includes the title property #}
      <div class="postTitle">
        <h2>{{title}}</h2>
        <p>
          <h5>{{date | htmlDateString}}<span class="divider"></span></h5>
        </p>
      </div>
      {# ADD P ELEMENT FROM TODO ON THE NEXT LINE #}

      {# Content from the body of the post markdown appears here #}
      {{ content | safe }}
    
      <div class="controls">
      {# The previous and next post data is added in .eleventy.js #}
      {% if prevPost.url %}
        <p>
          <strong>Previous post</strong>: 
          <a class="previous" href="{{ prevPost.url | PREFIX}}">{{ prevPost.data.title }}</a>
        </p>
      {% endif %}
      {% if nextPost.url %}
        <p>
          <strong>Next post</strong>: 
          <a class="next" href="{{ nextPost.url | PREFIX}}">{{ nextPost.data.title }}</a>
        </p>
      {% endif %}
    </div>
</div>
